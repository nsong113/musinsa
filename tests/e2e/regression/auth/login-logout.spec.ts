import { test, expect } from "@/fixtures/index";

test.describe('Login', () => {
  test('example login test', async ({ page }) => {
    // Arrange
    
    // Act
    
    // Assert
    await expect(page).toHaveURL(/.*/);
  });
});
