import { test, expect } from "@/fixtures/index";

test.describe("Like", () => {
  test("example Like test", async ({ page }) => {
    // Arrange

    // Act

    // Assert
    await expect(page).toHaveURL(/.*/);
  });
});
